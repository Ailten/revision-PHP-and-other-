
// --- SQL --- 

Postgresql  // server DB (like mysql).
datagrip  // soft for making request on DB (intelij, free for non-comercial, use with git acount).

SQL déclaratif :
    - DDL : data définition language (create table).
    - DRL : data retrivial language (select).
    - DML : data modification language (update).
SQL procedural :
    - function defined user
    - stored procedure
    - trigers

-- comment.
/* comment many lines */

SELECT * FROM `user`;  // select.
SELECT u.name AS name FROM `user` u;  // get only specific column and remap as name.
SELECT DISTINCT * FROM `user`;  // get without doublon.
SELECT COUNT(DISTINCT *) FROM `user`;  // get the amount of row.
SELECT * FROM (SELETC * FROM `user`);  // sub query.
SELECT * FROM `user` u WHERE (u.id = 5);  // make a filter.
=, >, <, >=, <=, <> (!=), BETWEEN, LIKE, IN, NOT, AND, OR  // type of comparator can be use.
SELECT * FROM `user` u ORDER BY u.id;  // make in a order.
SELECT * FROM `user` u ORDER BY u.id DESC;  // make in reverce order (ASK or DESC).
WHERE u.name IS NULL; IS NOT NULL;  // filter about null value.
COALESCE(u.name, "Anone");  // coalesce. (see also IFNULL() and ISNULL()).
CAST(u.age AS int)  // cast type.
CONVERT(int, u.age);  // same.
SELECT TOP 100 * FROM `user`;  // take only the 100 first elements return by the query.
SELECT TOP 50 PERCENT * FROM `user`;  // take only the 50% first of elements return by the query.
SELECT * FROM `user` OFFSET 10 ROWS FETCH NEXT 10 ROWS ONLY;  // skip the 10 first rows and take the 10 next only. (usefull for pagination)
LIKE 'Th___as'  // '_' can replace one character (patern with like).
LIKE '%as'  // '%' can replace zero or many character.
IN ('Thomas', 'Bob')  // set a list of value and compare if value is on it.
BETWEEN 5 AND 12  // set a range of value and compare if value is between (also can use NOT BETWEEN, or use to DATE).
HAVING COUNT(*) > 10;  // add filter (in a groube by query), heer take only group when it contain more thant 10 rows merged.
EXISTS  // if next value is nothing or null, return false.
ANY (sub query)  // return true if match any of the rows sub query.
ALL (sub query)  // return true if match all of the rows sub query.


SELECT * FROM `user` u GROUP BY u.age;  // select user grouped by age (can be use with COUNT or other function making many row as a single one).
COUNT, MAX, MIN, SUM, AVG  // function commun making singe row based on a group of row (AVG make a moyenne).


INSERT INTO `user` (id, name) VALUES (1, "Thomas");  // insert a new row.
INSERT INTO `user` (name) VALUES ("Thomas"),("Benoi"),("Bob");  // insert many row with a single line execution.
SELECT * INTO `user2` [IN `DataBase2`] FROM `user`;  // copy rows from a table into another table (can set another database destination).
INSERT INTO `user2` SELECT * FROM `user`;  // same.

UPDATE `user` u SET name = "Thomas" WHERE u.id = 5;  // edit a tow.

DELETE FROM `user` u WHETE u.id = 5;  // delete a row.
DROP TABLE `user`;  // delete a table.
DROP TABLE IF EXISTS `user`;  // same but only if exist.


CREATE TABLE IF NOT EXISTS `user`  // same as create a table, but only if not exist.
CREATE TABLE `user` (  // create a new table.
    `id` int(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,  // auto increment.
    `idCity` int(11) NOT NULL,
    `name` varchar(255) DEFAULT "Thomas",  // default value.
    `name` char(50) DEFAULT "Thomas",  // char type take all time the length send.
    PRIMARY KEY (`id`),  // primary key.
    FOREIGN KEY (idCity) REFERENCES City(id),  // forein key.

    // create the constraint only (based on the param create in amount like a regular param).
    CONSTRAINT `fk_class`  // name the constraint.
        FOREIGN KEY (`idClass`)  // refer the name of param FK in this table.
        REFERENCES `class`(`id`)  // refer table target (and his id). 
        ON DELETE CASCADE
        ON UPDATE CASCADE

) ENGINE=InnoDB AUTO_INCREMENT=6 DEFAULT CHARSET=utf8;  //type of engine and other parameter for table.

CREATE DATABASE `DB-name`;  // create a data-base.
ALTER DATABASE  // (?)
CREATE TABLE
ALTER TABLE

CREATE INDEX // (?)
DROP INDEX

SELECT * FROM `user` u JOIN `city` c ON u.idCity = c.id  // join (inner join).
SELECT * FROM `user` u LEFT JOIN `city` c ON u.idCity = c.id  // left join (take all row from table select, can return some null values).
SELECT * FROM `user` u RIGHT JOIN `city` c ON u.idCity = c.id  // right join (take all row from table join, can return some null values) (rarely used).
SELECT * FROM `user` u FULL JOIN `city` c ON u.idCity = c.id  // full join (take all row from both table, can return some null values) (rarely used).

SELECT * FROM `user` u1, `user` u2;  // self join (use to compare to same table).

(SELECT 'Thomas') UNION (SELECT 'Bob')  // merge two query result.
(SELECT 'Thomas') UNION ALL (SELECT 'Bob')  // same but allow doublon.


CREATE PROCEDURE `myProcedure`  // create a stored procedure.
AS  // can add a block begin/end.
SELECT * FROM `user`
GO;

EXEC `myProcedure`;  // call a stored procedure.

CREATE PROCEDURE `myProcedure` @myParam varchar(255)  // add a parameter.
EXEC `myProcedure` @myParam = 'Thomas';  // call a stored procedure with parameter.

DECLARE @myVar varchar(255) = 'Thomas'; // make a variable.

CREATE TRIGGER `myTrigger`  // create trigger on a table.
{BEFORE | AFTER}  // set if is execute before or after the main call.
{INSERT | UPDATE| DELETE }  // set on what type of call is called (can set many with OR between eatch).
ON `user` FOR EACH ROW
WHEN (new.no_line > 0)  // optional, can add a condition to call the trigger (?).
BEGIN
    PRINT 'hello';
END;


IF (...)  // if.
BEGIN  // start a new block (for query embed or if).
    PRINT 'hello'  // print something on console.
END  // end a block.
ELSE  // else.

CASE  // block switch case (can be use into a where or a select).
    WHEN u.name = 'Thomas' THEN 'hello Thomas'  // case.
    ELSE 'hello'  // default.
END


use MyDataBase;  // select a data base.
show tables;  // list all table of the data base.


sudo mysql -u root -p  // open cmd SQL (as user-sql root) and ask for password (sudo, and sql-root).
mysql -u [user] -p [DB-name] < [file-create-table.sql]  // en cmd, insert a file import/export DB.



// --- SQL Database (from w3c) ---



// example sql advenced for select 4 element (always) by matching value (or null).
SELECT v.valeur, i.*
FROM (
    SELECT 'a' AS valeur, 1 AS pos
    UNION ALL SELECT 'b', 2
    UNION ALL SELECT 'c', 3
    UNION ALL SELECT 'd', 4
) v
LEFT JOIN items i ON i.code = v.valeur
ORDER BY v.pos



// --- Postgresql.

create database [name-db];  // create a db.
\[name-db];  // checkout a db.

create table [name]();  // create a table.
create enum [name]('A', 'B');  // create an enum.
create domain [name] as int check(value > -1);  // create a type (with a constraint on it).
create collation [name](  // create a new type of strictness, for compare string with "like".
    provider='icu'
    locate='en-US-u-ks-level1'  -- change level for strict intencity (about char accent, or case sensitive).
    deterministic=false
)
alter table [name] add column [name-column] int (constraints);  // add a column to a table existing.
alter table [name] drop contraint [name-constraint];  // remove a constraint frop a table.
truncate table [name] [restart identity | continue identity];  // delete all row of a table (more opti), can restard PK increment, but not afect cascade on other table.
offset 10 limit 10  // on a select, skip 10 firsts row, and take 10 next row (usefull for pagination). 
case  // switch case.
    when [condition] then [value]
    else [value]
end
max(myValue) over()  // over make the camps call on eatch line (keep value, but undo the agregate effect).
max(myValue) over(partial by [param])  // can constraint a param specific (?).
extract(day from myTimeStamp)  // get day from a timestamp (can be a substract of two date, for usefull case).
@(myInt)  // absolute.
avg(param) over(partition by paramGroupBy)  // like a group by into a row specific (use for compare result agregate with a nimble champ).
lag(param)  // take the previous value (last row).
having param  // like a a where, but after goup by, to filter on agregate result (after agregate all values).
explain analyse  // place before a select (or else), to see in console the details of execution.
rank()  // create a column with value incremente (like an index from 1 to N, on the select it self).
dense_rank()  // same but don't skip num when two previous has the same value.
sum(param) over(order by param)  // make a sum on over, do a cumule progressifof value.
select distinct param  // skip row already find for this value (skip doublon).
\dt;  // show all table.

id uuid PRIMARY KEY DEFAULT gen_random_uuid()  // PK type more ofusced than int.
generate always as identity  // generate PK (?), prevent for set a value already set.
interval  // type "time", stock dif between two date (can be just stock 2 date).
time  // type.
timestamp  // stock secondes from 1970 (expire in 2038, so use timestampz.
timestampz  // same but bigger.
char, varchar  // type for stock string (varchar is size modulable, but + 1 byte).
double precision, real  // float type.
deciaml(x, y)  // decimal number (x = number of digit, y = number of decimal digit (include in x)), also this type prevent error of precision float.

constraint [name-constraint] check(age > 6)  // add constraint on params of table.
unique(key1, key2)  // can make a constraint define unique with many parameters.
primary key(key1, key2)  // can make constraint about primary key with many parameters.
foreign key [table2](pkOfTable2)  // define a foreign key as constraint.
on delete [cascade | set null | set default]  // define how it suposed to be proced, when the table of the FK is deleted.
on update [cascade | set null | set default]  // same for update (less used).

string_agg(myStr, '')  // concat many string in one (use with group by).
concat_ms('separator', str1, str2)  // join, concat many string with a separator.
myStr || myStr  // concat two string.
substring(myStr from 0 for 5)  // substring (index start, quantity to take).
ltrimp, rtrim, trim  // functions to trim a string (remove space in extremity).
left, right(myStr, 4)  // take 4 first (or last) char.
position('i' in 'aei')  // return the index of a substring on a string.
start_with(myStr, 'abc')  // verify if start with a substring.
myStr like '%a%'  // like regex for sql, match the patern (% = all char 0~2 times).
myStr in ('A', 'B')  // includes in many values.
myInt between 5 and 10  // between two value (on a range numeric).
string_to_table('A|B|C', '|')  // split a string into table row.
raise notice '---%-', myParameter;  // throw a log (char % replace by the param send).

date_part('Y', myDate)  // get int value for years in an object date.
date_part('month', myDate)  // same with month.
to_char(myDate, 'YYYY-MM-DD')  // cast a date on string (format).
now()  // get a date represent now.
date('01 01 1993')  // build a date from formated string (mm-dd-yyyy).
'2024-11-08'::date  // process as date (?).
myDate between myDate and myDate  // between two date.
date_cmp(myDate, myDate)  // dif between two date (in secondes).
myDate - myDate < 5  // compare days dif.

myStr like 'a'  // equal.
myStr ~~ 'a'  // same.
myStr ilike 'A'  // equal with insensitive case.
myStr ~~* 'A'  // same.
myStr !~~ 'a'  // not like.
myStr !~~* 'a'  // not ilike.
myStr like myPatern collate myStricness  // use like with a strictness of comparate (create).
!=, <>  // not equal.
'_'  // equal to one character (any type).
'%'  // equal to many character (any type).
'^@'  // equal start of string (?), prefer use "similar to" for regex logic.
similar to  // like, but take regex patern.
~  // same.
'a|b'  // or.
'a*'  // many time (0~n).
'a+'  // many time (1~n).
'a?'  // many time (0~1).
'a{1,2}'  // many time (choose min and max).
'()'  // group.
'[a-z]'  // range.
'^$'  // start and end (not sure).
substring('myStr' similar 'patern' escape 'replace')  // replace (?).
regex_count(myStr, patern)  // count match.
regex_count(myStr, patern, 5, 'i')  // same with index start and case insenssitive.

inner join  // take inter section of A and B (horizontal joins).
join  // same.
left join  // take A and B (or null if no B).
right join  // take A (or null if no A) and B.
full outer join  // take both A and B.
union all  // take both A and B (vertical joins).
union  // same.
exept  // take A.
intersect  // take intersection of A and B.
cross join (select ...)  // like a join, but allow to do where and LIMIT on it to optimise it.

where myParam > any (select ...)  // "any" make a comparaison (>,=,~) on many values (sub query), return true if at least one values respect the operator.
where myParam > all (select ...)  // same but return true if all values repsect the operator.

with my_function_cte_name as (  // build a query stocked (like a function), usefull to call like a subquery (CTE, common table expressions).
    select ...
)
), my-temporale-table as (select ...)  // at end of a cte, alow to store value on a temporal table.
with my_function_cte_name(n) as (  // allow parameter (?).
with recursive my_function_cte_name as (
    select e1.id  -- first call.
    from employees e1
    where e1.next is null
    union 
    select e2.id  -- second call (recurcive).
    from my_function_cte_name r1
    join employees e2 on (e2.next = r1.id)
)

create temp table (myParam1 int, myParam2 int) as myNameTableTemp;  // stock result of a query on a table temporary.
insert into myNameTableTemp(select ...);  // fill a temporary table.

do $$  // function in postgresql.
declare
    myVar := 5;  // declare variable used in function.
begin
    select ...
end $$ language plpsql

select myValue into myVariable from ...  // set a value into a variable.

create or replace function myFunctionName()  // function with python on it.
return table (myParam1 int, myParam2 int) as
??
begin
    ...  // python code.
end $$ language plpython3u;

create or replace view myNameView as select ...  // create a view (usefull for filter data not allow to access).

CREATE TABLE myschema.mytable (  // schema (Schemas are analogous to directories at the operating system level, except that schemas cannot be nested). 
 ...
);

create index myIndexName on tableTarget(paramTarget);  // create an index on a table (gain optimisation for axess data by where).
create unique index ...  // index can be unique (if paramTarget is unique on table to).
create index myIndexName on tableTarget(paramTarget1, paramTarget2);  // can have two parameter (in this case, make it twice in both order).
// when use index, think to reorganise/rebuild it (mintenance DB server), with key words "reindex, vacuum, concurrently".

pgagent  // tool (to dl), for script mintenance/maj at horaire fix.

create procedure myProcedureName(myParam1 type, ...) language plpsql $$  // create procedure stocked.
begin
    if ...
    then
        select ...
    else
        ...
    end if;
end $$

call myProcedureName(param1, param2);  // call a procedure stocked.







