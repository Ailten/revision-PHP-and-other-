
using System;  // way to import library.
namespace MyNamespace {}  // create a block for a specific class to import.
// comment.  // comment.
/* comment. */  // comments many line.
///*  comments.  // comments documentation (upper a class or function).
Console.WriteLine("test");  // print a line on a console.
Console.Write("test");  // print a word on a console (without back return).
string userInput = Console.ReadLine()  // get text write by user in console.

public void MyFunc() {}  // create a function.
MyFunc();  // call a function.
public void MyFunc(int a) {}  // parameter.
public void MyFunc(int a = 5) {}  // default value parameter.
public bool MyFunc() { return true; }  // return value.
public void MyFunc(params int[] arr) {}  // various amount of parameters.
MyFunc(a: 5);  // function can be call by naming the parameter (allow to send in what order you whant).
(param) => {}  // lambda.
yield return myValue;  // on a loop, allow to return a list of value one by one.
public async Task<int> MyFunc() {}  // async task (function async).
Task<int> myFunc = MyFunc();  // execute the task (and stock it in a bloc).
myFunc.Wait();  // wait until the task async be finish.
myFunc.Result  // get the result (return) of the task call.
await MyFunc();  // call an async function, with waiting it.
Thread.Sleep(5);  // make wait 5 secondes.
// todo : async, await, Delegate, Action, ...

int myInt = 5;  // create an int (number integer, big capacity).
short, byte  // type integer more short (60K and 255).
uint, ushort, ubyte  // type integer fully positive (not under zero).
float, double  // type for number with decimal (can have error of precision), float are less expencive in memory, but less size.
decimal  // type for number decimal (more exact, and more expencive memory).
0.5f, 0.5d, 0.5m  // hardcoded value decimal in float, double, and decimal.
char, string  // type for character, and chain-character.
bool  // type for boolean.
null, int?  // value null.
object  // the default-est type (can englobe all type, by eritage/polymorphisme).
(int) myFloat  // cast float variable into an int (can make by default, in this case).
T  // generic type (define on situation).
typeof(myVariable)  // return a value Type of the variable send.

+, -, *, /  // operator.
++, --  // increment, decrement (before or after the variable).
%  // modulo.
+=  // operator and afectation.
>>, <<  // decalage byte value.
&&, ||, ^  // and, or, xor (can work on boolean or byte value).
>, <, >=, <=, ==, !=  // comparator.
Math.Min, Max, Abs, Round, Floor, Ceil, Sqrt, Pow  // math functions.
??  // coaless.
?.  // do next only if left part is not null.
!.  // do next, don't worry if the left part can be null, I know it's not gonna be (by pass compilator error).

"A".ToLowerCase()  // make string lowercase.
"a".ToUpperCase()  // make string uppercase.
"a" + "b"  // concatenation.
$"a {myString}"  // concatenation (other methode).
"a"[0]  // accesser to a char.
"a".Subtring(0, 1)  // get a part of a string.
"a".IndexOf("a")  // get index of first match.
"a".StartWith("a");  // retrun true if start with.
"a".EndsWith("a");  // return true if end with.
"a".Replace("a", "b");  // replace a part of string by another.
"a".Remove(0, 1);  // remove a part of string (return the new string).
"a.b".Split(".");  // cast strign into array of string (cut at ocurence find).

if(...) {}  // if.
else if(...) {}  // else if.
else {}  // else.
(...)? "a": "b"  // ternaire.
switch(value) { case "a": /* something */ break; default: /* else */ }  // switch case.

while(isStay) {}  // while.
do {} while(isStay);  // do while.
for(int i=0; i<5; i++) {}  // for.
foreach(string cel in arr) {}  // foreach.
break, continue  // break, continue.

string[] arr = {"a", "b"};  // array.
arr[0]  // get value of a cell of array.
string[4] arr;  // define the size of array (without value).
string[,] arr2D = {{"a"}, {"b"}};  // array 2D.
Array.Sort(arr);  // sort a array.
List<string> myList = new List<string>(){ "A", "B" };  // list.
myList.ToArray();  // cast list into array.
myList.Select(cel => cel[0]);  // cast all element of a list into another list of other elements.
myList.Contains("A");  // verify if list contain an element.
Dictionary<int, string> myD = new();  // dictionary (list of KeyValuePair).

public class MyClass {}  // create a class.
MyClass mc = new MyClass();  // instanciate a class.
public MyClass() {}  // constructor.
public struct MyStruct {}  // struct, instance like class, just instance are distinct at all afectation.
public, private, protected  // access level.
static  // make not instance distinct.
abstract, override  // key word for alow to override, and override (in parent, and child).
abstract class MyClass {}  // abstract make a class not instanciable (only can be parent for other).
public int myInt { get; set; }  // define if a variable param is access or setable.
public class B : A {}  // eritage (B is child of A).
public B() : base() {}  // call the constructor parent.
base.MyFunc();  // call function of parent.
interface IMyInterface {}  // interface, like a class not instanciable, but only for method.
public class MyClass : InterfaceA, InterfaceB {}  // class can exprend of many interface.
enum MyEnum { A, B }  // create an enum (all value can be assign an int value unique), can have set index with "=".
MyEnum.A  // get value of an enum (can be cast as int).
public statinc string MyFunc(this MyEnum myEnum) {}  // can inject a method in an enum.

using System.IO;
string docPath = Environment.GetFolderPath(Environment.SpecialFolder.MyDocuments);  // get path to Documents folder.
using(StreamWriter outputFile = new StreamWriter(Path.Combine(docPath, "myDoc.txt"))) {}  // open a streamWriter to write on a doc.
outputFile.WriteLine("my line");  // write on a streamWriter.
DictoryInfo folder = new DirectoryInfo("path");  // get an object folder.
folder.GetFiles("*.txt");  // get files obj from a folder.
using(StreamReader sr = File.OpenText("path.txt")) {}  // open a file to read it.
while((s = sr.ReadLine()) != null) {}  // read all line one by one (need a string nullable "s" create before).
string jsonStr = JsonSerializer.Serialize(myObject);  // cast an obj into a string.

try {}  // try.
catch(Exception e) {}  // catch.
finally {}  // finally.
throw new Exception("message");  // throw error.
public class MyError : Exception {}  // create a custome error.

Random rng = new Random(DateTime.Now.Milliseond);  // create an obj random (need a seed).
rng.next(2);  // return a random number between 0 and 1 (include, integer).

using System.Text.RegularExpressions;
Regex rgx = new Regex("[a-z]+");  // regex.
string output = rgx.Replace(myStr, myReplaceChar);  // replace.
bool isMatching = Regex.IsMatch(myStr, rgx);  // eval if is matching.
Match[] ms = Regex.Matches(myStr, rgx);  // get objects match (contains all match part of string).
m.Value, m.Index  // get value match, and index.

DateTime  // date time instance.
DateTime.Now;  // get an instance DateTime at now.
CultureInfo c = new("en-US");  // create a format for date cast ("fr-FR").
myDate.ToString(c);  // cast a dateTime in a format language.
myDate.ToString("d", c);  // same, but ask for date only (no hours, minutes, secondes).
myDate.AddDays(1);  // add one day to a dateTime instance.
myDateA > myDateB  // compare two date.
TimeSpan dif = myDateA - myDateB;  // get a timeSpan dif between two dateTime.
dif.TotalHours  // get the all amount of hours between on a dif two dateTime.
StopWatch sw = new StopWatch();  // create an obj StopWatch (to chronometre the time delay of execution).
sw.Start();  // top start the chrono.
sw.Stop();  // top end the chrono.
sw.Elapsed  // get the amount of time the chrono catch.

DbContext  // use as ORM for Entity (on project DB), need to be try.
Context ctx = new Context();  // obj used to manage all Entity (ORM).
ctx.MyEntities.Add(new Entity);  // set an entity on the context ORM.
ctx.SaveChanges();  // save the changes.

#if DEBUG  // block "is executed on debug mode".
#else  // block else (executed on prod).
#endif  // end if.
#region my block of code  // create a custom block named.
#endregion  // end the block.

GC.SuppressFinalize(myObj);  // force destroy an obj.
GC.Collect();  // force garbadge collector to collect now.
GC.WaitForPendingFinalizers();  // wait the end of garbadge collector do.

[Flags]  // add the tag "Flags" on a class/enum (flags allow instance to have many value).
MyEnum me = MyEnum.ValueA | MyEnum.ValueB;  // example of assigne two value of a same instance with attribute flags (prefer use an array).
me.HasFlag(MyEnum.ValueA);  // verify if instance has a specific value (flag).
public class MyAttribut : Attribute {}  // create a class attribute (can be used on other class), allow pass a string in constructor etc.

// todo : unit test.

// continue : https://github.com/Ailten/CSharp-Test/blob/main/Exo22/Exo22.cs