    
    https://learngitbranching.js.org/?locale=fr_FR


    help -> Affiches les pages de tutoriel de l'application
    help levels -> Affiche les consignes du niveau actuel
    levels -> Affiche la liste des niveaux
    reset -> Réinitialise le niveau actuel
    show commands -> Affiche toutes les commandes disponibles
    show goal -> Affiche l'objectif du niveau actuel
    show solution -> Affiche la solution du niveau actuel


    branch
    checkout (deprecated, utilisez switch)
    cherry-pick
    commit
    merge
    rebase
    reset
    revert
    switch


    HEAD = name of branch currently checkout.
    hash = node of all commit in branchs project.
    tags = park a node git as Constante (can't be changed) and mark a clef-point in dev processuce.
    origin = base of arboressence of distante (depo dist).

------------ default cmd to setup on a new repository. (sugested by git)

git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Ailten/AdventOfCode_2025.git
git push -u origin main

------------

git commit   // send an edition of current branch.
git commit -a -m [comment]  // select all modif (-a) and add a comment (-m).

     v main*.
A -> B       // make B.


git checkout [nameBranchDestination]  // move to another branch.
git checkout -b [nameBranchDestination]  // -b for create a new one.
git checkout -b [branchName] [o/main]  // make a new branch (set name) and tel her to follow origine (at distance).

     v main local.
A -> B
     ^ second branch local*.


git merge [brancheWithEdition]  // make a new node with the current checkout (and merge the one send).

          v main*.      
A -> B -> C -> E  // merge from main using D as parameter. 
      \       /
       -> D ->
          ^ second branch local.

git rebase [brancheDest]  // add modification of current checkout into destination.

               v main (dest rebase).
A -> B -> C -> E
      \       
       -> D
          ^ branch edited*.

------------

git log  // print ID of hashNode.

git checkout HEAD^  // comme back one node upper HEAD (in hashNode).
git checkout HEAD~2  // comme back 2 nodes upper HEAD (in hashNode).
git checkout HEAD^2  // comme back 1 nodes upper HEAD but the second parent (first was the direct one).
git checkout HEAD~2^2  // can be chaned (heer, go 2 node upper, and right after on the second parent).
git checkout -b [nameBranchToMake] [destinationBranchToGo]  // make a new branch at a specific position.
git checkout -b [nameBranchToMake]  // same, with the branch curretly checkout.

git branch -f main HEAD~3  // force to replace current checkout has 3 node upper HEAD.
git branch -f [branchTarget] [branchDest]  // more readable.
git branch [newBranchName] [branchDest]  // can be use to create a new branch at the destination ask.
git branch -u [o/main] [branchName]  // configure a branch and make it follow origin/main.

          v (move branch [firstParam] into [secondParam].
A -> B -> C
      \       
       -> D

git reset HEAD~1  // cancel the commit, and go back to node parent.

     v go back.
A -> B -> C

git revert HEAD  // make a commit with the modification of cancel last commit.

              v go next (roll back).
A -> B -> C > C'

git cherry-pick [commit1] [commit2] [...]  // move all commit send under the current checkout.

          v main*.   v branchs moved.
A -> B -> C -> D' -> E'
      \       
       -> D -> E

git rebase -i [branchToStart]  // rebase interactif (open a vim menu to edit the order of rebase, and other option).
git rebase -i [branchToStart] [branchToMove]  // two parameter (more readable).

A -> B -> C
      \       
       -> C'
          ^ branch param re-create (by passing param B).

git commit --amend  // edit the last message commit.

          v checkout*.
A -> B -> C
      \       
       -> C'
          ^ new commit recommented.

git tag [tagName] [branch]  // mark a branch with a name tag (tag name can be use into other commend parameter).

     v (myTagName)
A -> B

git describe [branch]  // print a string as the path of the node (nameTag, node name and hash code).


------------


git clone  // cmd for clone a depo distant into a local one.

git add *  // add all files (except the one in .gitignore).
git commit -a -m "..."  // commit the edit and make a comment.
git push  // send the modification to the distance.

git fetch  // get data from distance one (from distance to local) (of all branch) (BUT fetch don't change the local value of files).
git merge [origin/branch]  // fusionner the branch ask (get value of branch ask and fusionne to the one checkout) heer the origin branch is merge to the local checkout.
git pull  // make a fetch and a merge (get the dist branch to local, and fusionne values).

git merge [branchBForFusion]  // fusionne the checkout with the parameter branch, in a new node (under the checkout).


git fakeTeamwork [amountOfCommit]  // !! only for exercice (not a real command).


git fetch; git rebase [o/main]; git push  // 3 action to get change from distance (without your local changes).
git fetch; git merge [o/main]; git push  // 3 action to get change from distance (with your local changes).
git pull --rebase; git push  // short cut for fetch and rebase (without your local changes).
git pull; git push  // short cut for fetch and merge (with your local changes).

               v pull --rebase (result).
A -> B -> D -> E
      \
       -> C
          ^ new commit local*.

               v pull (result).
A -> B -> D -> E
      \       /
       -> C -
          ^ new commit local*.

git push [origin] [branchLocal]  // push to distance by sending param.


-- rebase -> garde l'arbre proche (ligne droite).
-----------> MAIS, modifie l'historique des commits.

-- merge -> make a all branch point to a "result".


git checkout -b [nameBranchToMake] [destinationBranchToGo]  // make a new branch at a specific position.
git checkout -b [nameBranchToMake]  // same, with the branch curretly checkout.
git branch -u [o/main] [branchName]  // configure a branch and make it follow origin/main.


git push [origin] [branchName]  // go to branchName, and push the contend to origin/main (distance).
git push [origin] [branchName]:[branchDest]  // same but precise in what branch dest push it 
                                             // (take branchName as what destination in local, and Dest as what branch in distance one (can be a new name).
<source>:<destination>  //  block of two param, for specify source and destination.
git fetch [origin] [branchName]  // get all changes from branchName (distance) and place it in branchName (local), like a pull specific.
git fetch [origin] [branchName]:[branchDest]  // pull branchName (distance) in name branchDest (local).
                                              // branchName is node distance, branchDest is what name move in local.
git fetch  // without arguments, act like a full pull (of all branh).


git push [origin] :[branchDest]  // erase the branchDest (from local and origin).
git fetch [origin] :[branchDest]  // create branchDest at node checkout (in local).
git pull [origin] [branchName]:[branchDest]  // make a pull of branchName (in distance) into branchDest local (can be a new branch created).


git rm [file]  // remove a file (from the current project).
git rm -r [folder]  // remove a folder (-r to recursive on all file/folder into it).
git rm -r --cached [folder]  // --cached, to remove from the distance branch checkout (not localy), usefull for a file marked on gitignore.

----------->


// Senssible case : how to rebase a branch/project fully diferent.
git checkout [myNewBranch]  // (-b for create it).
git merge [origin/main] --allow-unrelated-histories  // get the main branch from distance.
git checkout [main]
git merge [myNewBranch]  // merge main into newBranch. (warning, they have both files).


// delete a branch.
git branch -d [myBranch]
git branch -D [myBranch]  // force to.
git push origin --delete [myBranch]  // delete a branch distante (after a merge).



----------->



// build a backup in a new branch (and reset the main).
git checkout -b [name_branche_backup]  // checkout (and create) a new branch localy.
git push -u origin [name_branche_backup]  // push (to dist: origin) the branch.
git checkout main  // checkout back the main branch.



----------->



// .gitignore
file.txt  // igniore a file from all commit.
myFolder/  // slash precise to git it's a folder, and should igniore all stuff into it.
**/file.txt  // ** precise that file can be found in all type of path.
file.*  // * can match all type of text (in the same file/folder name).
file.[a-z]  // [] send a range of char can be match.
file.tx?  // ? can match a single character.



----------->



git stash -u  // for update file after a checkout really diferente.




----------->



// file [README.md] for make info about a git project (printable in repo github), Doc.
// work like html (p, a, align="center", href="...", img src="...", width="20",

## my test  // make title (bigger).
- my point  // make a list dotted.
[my link](http:...)  // make a link.
** my bold **  // make test bold (bigger).
```js my code ```  // a block of code (like in discord).



----------->



working-tree  staging  historique  |  remote
+----------+  +-----+  +--------+  |  +----+
|          |  |     |  |        |  |  |    |
|          |  |     |  |        |  |  |    |
+----------+  +-----+  +--------+  |  +----+
 where wee     temp     historic      pushed
   work        add      comited

     --------->   ------->    --------->
         add       commit        push

     <---------               <---------
     rm --cached                 fetch

     <--------------------
   merge / cherry-pick / reset

     <----------------------------------
                    pull

git log  // print all commit in historique (local).
git status  // print status of branch (commit need or not).
// git reset, for roll back work from historique (?).
git config (--global) user.name Ailten  // for edit config (set user name), can be do in folder git hidden in project.
git config (--global) user.email ...  // same, set email.
git update-index --assume-unchanged [file]  // mark a file as "not change from commit" except the first time when add.
// squash + push force  // erase all historique, and commit for erase data in historic.
git reset HEAD~1  // re-place index to an anterieur commit (rollback one commit).
git revert ...  // same as revert, but take changes (for not have to force push).
// git.md  // file for description project.
git stash  // make a temp "pseudo branch" local, for split working task.
git stash apply  // get back data (from before a stash + commit).
git stash list  // print all stash.
git stash pop [index]  // remove a stash.
git stash apply [index]  // back to a stash specify.
git stash clear  // remove all stash.
git remote rm origin  // delete branch origin from remote.
git remote add [branch-name] [url]  // setup a remot git project.
git push [branch-name] [main]  // push a branch send to main.
git fetch [branch-remote] [main]  // syncronise historic.
git merge [branche]/main  // fusione branch send with historic vertion (commit, but not pushed).
git push [remote-branch] [branch]  // syncronise local, to dist.
git push [remote-branch] --all  // same, for all branch.
git fetch [remote-branch] [branch]  // syncronise dist, to local (without modif working tree).
git pull [remote-branch] [branch]  // same, but update working tree.
git merge [branch]  // fusione working tree on a historic branch.
git branch -u [branch]  // rename a branch (verify parameters of cmd before use).
git tag [name-tag]  // place a tag on a commit (like a vertion name, for target easily this commit).
git squash rebase  // erase a commit into another one (or many into another one).
git rebase -i HEAD~3  // rebase 3 commit upper.
git checkout -b [name]  // create a branch, and checkout it.
git rebase HEAD~x  // using letter to make other action (s for squash, r for reword, ...)
git checkout -b [branch-name-local] origin/[branch-name-dist]  // pull (and checkout) a branch from distance to local.



// -------- SSH key.

https://dev.to/hbolajraf/git-connecting-to-github-and-pushing-changes-using-ssh-on-windows-2f5
ssh-keygen -t ed25519 -b 4096 -C "your_email@example.com"  // create a new SSH key. (no file, password in passphrase).
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa
// place the contend of file ".ssh/id_ed25519.pub" into github profile key.
ssh -T git@github.com


// ------ clone and move in a project.

//clone a repo git has a lib.
git clone https://github.com/RobLoach/node-raylib.git  //clone le repo sur le PC. (nimporte quel emplacement cmd).
mv node-raylib /path/to/your/project/  //déplacer le repo clonné dans le projet.


// --- advice, switch branch.

git switch [nom branche] //switch to another branch (checkout).
git switch -c [nom new branche] //duplic current branch checkout into new one.






