
// --- JS ---

// comment single line.
/* comment many line. */
"use strict"  // a string set all up of a JS script, to make it force use strict syntax.
console.log("test");  // write in console.
alert("test");  // pop a window with a text on it (and a button "ok" to exit).
let input = prompt("question");  // pop a window with a champ to user enter text before validate.

console.error("test");  // write in console (as an error, in red).
console.warn("test");  // write in console (as a warning, in yellow).
let isValidate = confirm("question");  // pop a window with choise "ok" or "cancel".
console.clean();  // clean the console.
document.write("test");  // write on the html contend (erase the all before page).
console.assert(boolean, "message");  // print in console, if the boolean send is false.
console.count("myTag");  // print in console "myTag : {num}", as num is the quantity "myTag" was call with this string.
console.group("groupName");  // start a group console (make a group including all next console.log).
console.groupEnd();  // end the scope group.
console.groupCollapsed("groupName");  // same as group, but make it collapsed (not unfolded).
console.info("test");  // same as log.
console.table([]);  // print an array or a object in console.
console.time();  // start a chronometre (fo eval performence).
console.timeEnd();  // end the scope chronometre (and print it).
console.trace();  // print in console the path of all function call to it (already print on error and log).

let myVar = 5;  // set a variable (disapear in end of scop).
var myVar = 5;  // set a variable.
const myConst = 5;  // set a constance (can't be edited value).

typeof myVar  // return the type of something.
Null  // value when something is null.
undefined  // value when something is not found properly.
Infinity  // value to represent the infinity numeric.
==  // compare equality (value only).
===  // compare equality (value and type)
!=, !, <, >, <=, >=, &&, ||, ??, 
myVar instanceof Array  // check if is an array (check type alternative).
parseInt("5")  // cast a string in a number (integer), or NaN.
parseInt("FF", 16)  // cast string hexadecimal (base 16) in a number.
parseFloat("5.2")  // cast string into number (float).
Number("5")  // cast string into number.
Infinity, -Infinity  // value theoricly infinit (as number).
myNumber.toFixed(2)  // define the amount of decimal take to the number.

document.getElementById('demo').innerHTML = "a";  // edit DOM text contend (by geting by id attribute, return one element or undefined).
document.getElementById('demo').innerText = "a";  // get the contend (without balises html).
document.getElementById('demo').outerHTML;  // return the balise with balises html.
document.getElementById('demo').style.backgroundColor = "red";  // set CSS by the DOM.
document.getElementsByClassName('demo')  // get dom by class (return a array of element).
document.getElementsByTagName('demo')  // get dom by name balise html (return array).
document.getElementsByName('demo')  // get dom by matching to attribute "name" value (return array).
document.querySelectorAll("p")  // get dom by a query (like css selector), return a collection.
document.querySelector("p");  // same as querySelectorAll but take the first element of the collection.
window.addEventListener("load", (evnt) => {});  // set an event on window, trigger when it load end (see obj evnt in console for more details).
window.onload = (evnt) => {};  // same (other syntax).
window.removeEventListener("load", myFunc);  // remove the event of an element (need to pass the same function, so pass it named, not has lambda).
// Jquery : you can import a jquery.min.js in your page, to user jquery syntax $(myQuertSelector).
myDiv.offsetWidth;  // get width of element.
myDiv.getBoundingClientRect().width;  // get an object with all details of posisition and size on the page.
document.body  // return the balise body.
document.forms  // return all balises form of the page (collection).
document.scripts  // return all balises script of the page (collection).
myDiv.childNodes  // return the childs of the element (collection).
.firstChild .lastChild  // return the first or the last child of the element.
.nextSibling .previousSibling  // return the next of the previous element.
myDiv.parentElement  // return the element parent.
myDiv.hasAttriubte("myAtr")  // return true if element has attribute.
myDiv.getAttribute("myAtr")  // return the value of the attribute on the element.
myDiv.setAttribute("myAtr", "value")  // set attribute on an element.
// attribute custom should start by "data-" for nomenclature (optional).
myDiv.classList  // axess to the list class element (to contains, add, remove, switch, toggle).
window.getComputedStyle(myElement)  // return the css of an element (not tested).
myInput.value  // get value of an input.
myCkeckbox.checked  // true if a checkbox is checked.

"...", '...', `...`  // way to set a string value.
'..' + '.'  // concat.
`...${myVar}`  // concat with placeholder.
'...'.length  // get length of a string.
'...'[0]  // get char at an index send.
'...'.substring(1, 2)  // cut a part of string (index start, index end).
'...'.substr(1, 2)  // cut a part of string (index start, length take) depreciate !.
'...'.slice(1, 2)  // cut a part of string (like substring, but can take parameter negative).
'...'.split(regex)  // cast string into array (send param the mark use as cut).
'...'.toUpperCase()  // cast in upper.
'...'.toUpperCase()  // cast in lower.
'...'.trim()  // remove space at start and end.
'...'.replace("A", "B")  // replace the first ocurence of A by B (can take a regex).
'...'.replaceAll("A", "B")  // same as replace, but to all ocurence.
'...'.indexOf("A")  // return the index of "A" in string.
'...'.lastIndexOf("A")  // same as indexOf, but take the last.
'...'.search("A")  // same as indexOf.
'...'.includes("A")  // return true if string contain "A".
'...'.startWith("A")  // return true if string start by "A".
'...'.charAt("a")  // return the index wear was found the first char send (same as indexOf but for one char).
'...'.charCodeAt(5)  // return the ascii number of the char at the index send.
String.fromCharCode(64);  // return char '@', based on his ascii value.
encodeURIComponent("myUrl")  // return a string (uri) by casting the special char for work on url).
decodeURIComponent("muUrl")  // same but reverce.
myVar.toString()  // cast into string.
new Intl.NumberFormat();  // instancie an object to define the format of a number (spacing, virgule, € signe, decimal ...).
new RegExp("myRegex", "g")  // make a regex (instanciate syntax, for start from a string).
/^[a-z]$/  // set a regex (to one char betwin "a" and "z").
/^[a-zA-Z]$/  // same include maj.
/^[a-z]$/i  // same (i for take lower and upper case.
/^[0-9]$/  // regex 0 to 9.
/^[0-9]{2,5}$/  // same but betwin 2 and 5 digit.
^  // mark as start string.
$  // mark as end string.
(a|b)  // mark as "a" OR "b".
/g  // g for find all ocurence.
// see regex recap for more details on regex syntax.
/a/.test("")  // return true if the string match the regex.
"myStr".match(/a/)  // return an array of matching parameter, can return null if no match at all.
"myStr".replace(/a/, "b")  // replace all ocure of regex match in the string, by "b" (you can add $1-$9) in the string "b" as placeholder for matching value.
/a/.exec("myStr")  // return the part string find by regex.

if(...) {}  // if.
else if(...) {}  // else if.
else {}  // else.
(...? A: B)  // ternaire.
switch(...) { case ...: break; default: }  // switch.
while(...) {}  // while.
do {} while(...) {}  // do while.
for(let i=0; i<length; i++) {}  // for.
for(let i in array) {}  // for (on an array), take the index not the value (on a dictionary, get the key).
[...].forEach(e => {});  // foreach.
break;  // break, exit loop.
continue;  // continue, exit current iteration.

let myAr = [2, 5]  // set an array.
myAr[0]  // get first element of array.
myAr.length;  // get length of array.
myAr.push(6)  // add a new element at end array.
myAr.unShift(6)  // same but place at left of the array (can take various amount of parameters).
myAr.pop()  // drop the last element.
myAr.shift()  // drop the first element.
myAr.every(() => true)  // loop on every element and return true if all return true.
myAr.some(() => true)  // return true if at least one return true by lambda.
myAr.fill(null, 1, 1)  // fill all element with "null", from index 1 to index 1 (not include).
myAr.filter(() => true)  // filter the element who return false by the lambda.
myAr.find(() => true)  // return the firt element who return true by lambda.
myAr.findIndex(() => true)  // same but return the index (not the element).
myAr.findLast(() => true)  // same but return the last (not the first).
myAr.forEach(() => {})  // loop on array to make a proces for eatch element.
myAr.includes('...')  // return true if contain the element send.
myAr.join(",")  // concat element into one string.
myAr.keys()  // return all keys of array in another array (usefull with dictionary).
myAr.map(() => {})  // cast eatch element in a new array.
myAr.reduce((cumul, e, i) => cumul+e)  // loop on eatch element and cumul on a variable the result at eatch passage one by one.
myAr.reduceRight()  // same but begin from the last one.
myAr.reverse()  // return a new array backward.
myAr.slice(2, 4)  // return a short peace of the array (start at index 2 end at index 4).
myAr.splice(2, 4, null)  // replace a peace of array by the value send (null) (2 index start, 4 length element replace, null value inserted (only once)).
myAr.indexOf("myValue")  // retourn the first ocurrence of this value on the array (return the index).
myAr.lastIndexOf("myValue")  // same but return the last.
myAr.sort()  // order the array element.
myAr = myAr1.concat(myAr2)  // merge two array in one (return the new), can take more than one parameter array.
Array.isArray(myAr)  // return true if is an array type.
new Set()  // array who can only have be add element and not twice the same (not usefulle).
new Map()  // array with key value pair (like dictionary).

Math.round, .floor, .ceil, .trunc  // round number decimal.
Math.sign  // return if the value send is under or uper zero (return -1, 0, 1).
Math.PI  // pi (see also E, LOG2E, SQRT2... etc).
Math.random  // return a number decimal between 0 and 1 (not include).
Math.abs  // return the value absolute (positive) of a number.
Math.pow  // do exposent.
Math.sqrt  // do square root.
Math.sin, .cos, .tan  // make sinus cosinus tangante (see also asin, acos, atan).
Math.min, .max  // return the min or max value from all value send in paramers.
Math.sign(myNum)  // return -1, 0 or 1 depend on the nefatif, 0 or positif of number send.
Math['myConst'] = 32  // set something in math object (use carfully to not make it trash).

function myFunc() {}  // set a function.
() => {}  // lambda (function short syntax).
return;  // exit of a function scope (can return a value).
function myFunc(a, ...b) {}  // heer, a function who take one parameter (a) and a various amount of other parameter (in array b), like Math.max.
myFunc(5, ...myArray);  // you can send directly the array (instread of all value one by one) like this.
myFunc.apply(null, [])  // call a function by sending parameter on an array (usefull to send a non-determined amount of parameters), you can replace null by the object owner of methode.
(function myFunc() => {})();  // function auto call.
// fermeture : act to make a function who return a function (and stock value on the function can be edited, like to make conter).
eval("myCode")  // execute a string as JS code (depreciate, big risk of injection and bad security issue).

let myObj = {}  // set an object.
let myObj = {name: 'Thomas'}  // set object with parameters.
myObj.name  // return a parameter of object.
myObj['name']  // same.
function Obj() { this.name = 'Thomas'; }  // make a function who return an object (alternative way).
let myObj = new Obj();  // instancie an object from a function.
class Obj { constructor() { this.name = 'Thomas'; } }  // class (syntax alternative).
class Obj extends ObjParent {}  // class eritage (set by a parent).
ClassParent.call(this, param1, param2);  // call the parent constructor (in prototype way, in class, just use super(param1, param2)).
static name = 'Thomas';  // attributes static (same value for all class).
delete myObj.myProperties  // delete the propertie of an instance (depreciate, can make spagety code with un-safe properties callable).
Object.values(Obj)  // return an array of values object.
Object.keys(myObj)  // return an array of key properties object.
Object.getOwnPropertyNames(myObj)  // return an array of properties (like keys, but return "length" to if it's an array).
Object.getPrototypeOf(MyClass)  // instancie an object by his type (usefull for instance generic.
Objecy.create(Object.prototype, {})  // instancie an object (replace Object.prototype by a class, and {} by the set of all parameters), see more in details.
Object.defineProperty(myObj, "myProperty", JsonDesc)  // add a property to an instance.
Object.entries(myObj)  // return an array of array (all cel is an array of key-value).
for(let [key, value] of Object.entries(myObj)) {}  // loop on every attributes of an object.
JSON.stringify(myObj)  // cast object into json string.
JSON.parse(myString)  // cast string json into object.
myObj instanceof Obj  // check if is an instance of Obj (like typeof).
tryFunc?.();  // execute a function only if it exist (if tryFunc exist but is not a function, it throw an error), need verification.
localStorage.setItem("key", value)  // save a value on local storage (a hard-memory on browser).
localStorage.getItem("key")  // get a value saved (can return null, or an error if execute without permission).

new Date()  // make a date (as now).
Date.now();  // same.
new Date("2022-03-25");  // by sending a string.
new Date(milisec)  // make a date by sending amount of milisec.
new Date(year,month,day)  // make a date by sending param.
new Date(year,month,day,hours,minutes,seconds)  // set all date param.
Intl.DateTimeFormat("fr-FR").format(new Date())  // return date string has a national format (can take an obj in second param to param type of data take).
date.getHours()  // get hours from a date.
date.getMinutes()  // same for minutes.
date.getDay()  // get index day of week.
date.getDate()  // get index day of month.
date.getMonth()  // get month.
date.getFullYear()  // get year.
date.getTime()  // get miliseconde from 1970 (date reference as 0 in IT).
myDate.setDate()  // edit a date obj to set the day of the month. (have all equivalence like get).
myDate.setTime()  // edit a date obj to set the time in milisec.
myDate.setFullYear("2000")  // set year on a date object.

try {}  // make a block to catch error.
catch(err) {}  // make a block to handle error catched.
finally {}  // make a block already call at end (like to close flux).
err.name  // get name of error.
throw 404;  // throw an error (can take a string).

async  // make a function asyncrone (when call, execute it in a second thread).
await  // force to call a function syncrone (when a function is async, call it sync).
setTimeout  // function to wait an amount of milisec before execute it (in async way).
setInterval  // same but call every eatch milisec interval.
Promise  // object returned from an async call (to know when it finish and get value returned).
clearTimeout  // cancel an execution call by setTimeout.
clearInterval  // same for interval.

window.open("url", "name", "option")  // open a new window (see more in details what kind of option : width, left, scrollbars ...).
myWindowPopUp.close()  // close a window opened.
myWindowPopUp.focus() .blur()  // afiche in first plan (or hidde).
document.getElementById("iframe").contentWindow  // get the window from a iframe (not verify).
window.navigateur.userAgent  // return a string of the navigateur used.
window.navigateur.onLine  // return true if the machine as access to internet.
window.navigateur.cookieEnabled  // return true if the user as unable cookie on the browser.
window.screen.width  // return the width of screen in pixel (not fiable in web, because window.innerWidth bether), work with height to.
window.screen.availWidth  // same but width available on screen cont only.
window.screen.orientation.angle  // return the angle of screen (?).
window.location.href  // full url of the page (can be affected to change page, or get as a string).
window.location.hostname .port .host  // return hostname (domain name), port, both.
.pathname .protocol .search  // path folder, http/https, data send in url (usefull).
.hash .origin  // char in url after "#", page who from the user to get in this page (usefull).
window.location.reload()  // reload the page.
window.location.replace("url")  // change the current page.
window.history.back()  // back to last page consulted (usefull).
window.history.forward()  // go to next page consulted (when user has make a back return).
window.history.go(-1)  // move index in historic navigation.
window.print()  // open the print view of the page.
window.resizeTo(x, y)  // resize the window.
window.resizeBy(x, y)  // resize the window (relative to the current size).
window.moveTo(x, y)  // move the position of window at screen (only use on pop-up scam, or crapy game).
window.moveBy(x, y)  // same but relative to current position.
window.screenLeft screenX posX  // get position horizontal of screen.
window.screenTop screenY posY  // get position vertical of screen.
window.scrollTo(x, y)  // scroll at a fix position.
window.scrollBy(x, y)  // scroll (base on current position scroll).

let objImg = new Image();  // instancie an obj image.
objImg.onload = () => {};  // event when it's load (verify documentation).
objImg.onerror = () => {};  // event call when img is an error to load.
objImg.src = "urlImg";  // set an url img (to start loading).
objImg.width .height  // get size of image (after loaded).

AJAX XMLHttpRequest fetch  // way to do call async (prefer use fetch).
fetch("url", {option})  // call async by fetch.
.then((result) => {})  // get result when it work succesfully (can have many then).
.catch((err) => {})  // get error returned.
let result = await fetch();  // call a fetch syncronously, and stock return.

let ctx = myCanvas.getContext('2d');  // get an object context of a canvas dom html (to draw on it).
ctx.beginPath();  // start a trace draw (start).
ctx.closePath();  // end a trace draw (end), make a last line from current virtual pos and the virtual pos where it was durring the beginPath.
ctx.stroke();  // apply the draw.
ctx.moveTo(x, y)  // move cursor virtual.
ctx.lineTo(x, y)  // make a line for current curssor virtual position, to next one send.
ctx.lineWidth = 5  // set size of line (thicness).
ctx.strokeStyle = "#0f0"  // set color of the line.
ctx.fillStyle = "#f00"  // set color of the fill.
ctx.setLineDash(pxFull, pxEmpty)  // define line style has dashed (optional, you can define the pixel repartition of full/empty).
ctx.clearRect(x,y, x2, y2)  // erase a rectangle on the canvas.
ctx.rect(x,y, x2,y2)  // draw a rectangle (work with fillRect to).
ctx.arc(x,y, x2,y2, Math.PI*2, isOlogeeDirection)  // draw a circle (of an arc circle), [posCenter, posBorder, radius, isOrlogee].
ctx.beginCurveTo(x2,y2, x3,y3, x4,y4)  // draw a bezier curve (need to try, not sur if it take the cursor virtual as first position or note).
ctx.drawImage(myImgObj, x,y, sizeX,sizeY)  // draw an image.
myGrad = ctx.createLinearGradient(x,y, x2,y2)  // create a gradient.
myGrad.addColorStop(i, "#00f")  // define colors of gradient (one by one), the i is a number between 0 and 1 include.
myGrad = ctx.createRadialGradient(x,y, r, x2,y2, r2)  // create a gradient radial (ciruclar).
myPatern = ctx.createPattern(myImgObj, "repetitionMode")  // create a patern (for repeating on canvas), [repeat, repeat-x, repeat-y, no-repeat].
ctx.fillStyle = myPatern  // define a patern to use as background for a fill geometry (fillRect).
ctx.strokeText("myTxt", x,y)  // draw a text (work with fillText to).
ctx.font = "20px arial"  // define the font size and the font of a text to draw.
ctx.textBalise = ""  // define how text need to be draw comparate to the position [alphabetic, middle, top, hanging, bottom].
ctx.mesureText("myTxt").width  // evaluate the size of the text before print, to ajust.
ctx.rotate(myAngle)  // rotate the canvas, to draw with a rotation.
ctx.setTransform(echelleX, inclineX, inclineY, echelleY, translateX, translateY)  // aply a transform to the canvas (never try).
ctx.save()  // save a current vertion of the context.
ctx.restore()  // rollback to the last vertion saved.
myCanvas.toDataUrl("image/png");  // get a string data of the image canvas (example, set on a <a> to make it download, or write it on a png file).
window.requestAnimationFrame(myFuncCallBack)  // function to animate a canvas eatch frame (you can use setInterval).
window.cancelAnimationFrame(myAnimationId)  // cancel an animation frame.

Chart.js  // lib to make graphics.
tree.js  // lib to make 3D.
Jquery  // to use $() syntax.
Bootstrap  // lib for CSS.
Angular  // framework JS (using TypeScript).
React  // framework JS (with a virtual dom, for actualise only what change).
VueJs  // framework JS.

// web worker : a way to execute JS script in background (when it's eavy), without blocking the user.
myWorker = new Worker("myFileJs.js")  // create a worker.
myWorker.postMessage(JsonMessage)  // send a message (can use to send a data to the worker: declare a "function onmessage(evnt)" on the file worker).
myWorker.terminate()  // destroy the worker.

// --- usefull stuff ---

getEventListeners(<DOMelement>)  // get list of event set to an element.
getEventListeners(<DOMelement>).click[0].listener  // get function call when click on element (the first one).
encodeURIComponent(<string>)  // cast special char for param url.

Math.lerp = (a, b, i) => { return a*(1-i) + b*i; };
Array.prototype.random = function() { return this[Math.floor(Math.random()*this.length)]; };

document.bodu.appendChild(document.createElement("div"))  // create a div and push in the body.
myDiv.insertBefore(document.createElement("div"))  // create a div and push before an element.
myDiv.removeChild(myDiv.getElementById("id"))  // remove an element. 
document.body.appendChild(document.getElementById("myElem").content.cloneNode(true));  // take "myElem" in the page, get his inner contend, duplicate, and push.

document.body.requestFullScreen();  // open full screen mode (full-full).
document.body.exitFullScreen();  // un-do the full screen mode.

myForm.submit();  // submit a form (work with all event on an element, like click, or other).
let evnt = new Event("mouseover");  // set an event to an objet to execute it with script (for event can be called directly).
myDiv.dispatchEvent(evnt);

window.getSelection().toString()  // get the selection text by user.
window.getSelection().getRangeAt(n)  // get an object Range, to manipule the selection (over many balise), obj Range can also insert node or do other stuff.

document.execCommand("copy")  // save in press papier the selection on the page.






